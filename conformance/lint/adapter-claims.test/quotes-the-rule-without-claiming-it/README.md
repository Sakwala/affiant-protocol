# @example/adapter-under-test

A corpus package for `conformance/lint/adapter-claims.mjs --self-test`. Not a real adapter.

CV-5 says no claim that a pause survives a process restart may rest on a feature that is not published under the `latest` dist-tag. Until a run proves otherwise, the Docket row alone is the durable state.
