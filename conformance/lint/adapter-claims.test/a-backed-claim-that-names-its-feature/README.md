# @example/adapter-under-test

A corpus package for `conformance/lint/adapter-claims.mjs --self-test`. Not a real adapter.

With the runtime's durable-execution, a pending approval survives a process restart and the run resumes in a new process.
